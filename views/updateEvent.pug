html(lang="fr")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
    body
        include ../partials/header.pug
        h1 Qui prend quoi ?
        h2 mise a jour de l'evenement
        form(method="post" action=`/party/${idParty}?_method=PATCH`)
            div
                label(for="name") Nom de l'événement
                input(name="name" id="name" required minlength="3" placeholder="Nouvel an" value=`${party.name}`)
                label(for="author") Auteur
                input(name="author" id="author" required minlength="3" placeholder="Monsieur Smith" value=`${party.author}`)
                label(for="date") Date
                input(name="date" id="date" maxlength="10" minlength="10" placeholder="YYYY-MM-DD" value=`${party.date}`)
            button(type="submit") Créer